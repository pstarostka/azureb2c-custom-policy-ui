# xml-parser

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build xml-parser` to build the library.

## Running unit tests

Run `nx test xml-parser` to execute the unit tests via [Jest](https://jestjs.io).
